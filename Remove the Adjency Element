public static String removeAdjency(String s)
    {
        String result ="";
        result += s.charAt(0);
        for(int i=1;i<s.length();i++)
        {
            if(s.charAt(i) != s.charAt(i-1))
            {
                result += s.charAt(i);
            }
        }
        return result;
    }
